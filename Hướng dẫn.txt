Hướng dẫn chạy bài lab:
	labtainer stego_code_bpcs_dct
chạy lệnh:
python3 pbcs_dct_embed.py
chạy lệnh:
ls
chạy lệnh:
ls -lh file(output) file(input)

chạy lệnh:
python3 pbcs_dct_extract.py

Chạy lại bài lab:

labtainer -r stego_code_bpcs_dct

